[project]
name = "relationship-engine"
version = "0.2.0"
description = "AI 关系管理 MCP Server - 任何 AI 都能调用的关系记忆引擎"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0",
    "pydantic>=2.0",
    "python-dotenv>=1.0",
]

[project.scripts]
re-engine = "src.main:main"
